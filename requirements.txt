# PyCoder — Python Coding Agent
# Core (Ollama backend)
pyyaml>=6.0
numpy>=1.24
requests>=2.28

# Web search & page parsing
beautifulsoup4>=4.12
duckduckgo-search>=5.0

# Optional: Transformers backend (not needed if using Ollama)
# torch>=2.0
# transformers>=4.40

# Optional: Better embeddings for long-term memory
# sentence-transformers>=2.2
